roomID (UINT)
nEffect(UINT) 
{
	classTypeSize(UINT) / classType(string)
	fileNameSize(UINT) / fileName(string)
	worldposition(float3)
	worldScale(float3)
	worldRotation(float4)
}

nObstacle 
nItems

nNextRoom(UINT)
{
	
}
